# Arc Mainnet (chain id 1243) — official RPC published at launch (16 Sep 2026)
ARC_MAINNET_RPC=
ARCSCAN_API_KEY=

# Deployer key is NOT an env var. Use an encrypted Foundry keystore:
#   cast wallet import archer --interactive
# then deploy with:  forge script ... --account archer
